<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:radius="10dp"
        />  <!-- Adjust the radius value as needed -->
    <solid
        android:color="@color/shimmer_light_gray" /> <!-- Optional: Set a background color -->
    <stroke
        android:width="2dp"
    android:color="@color/shimmer_light_gray" />
</shape>